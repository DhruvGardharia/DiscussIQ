// OtpVerify removed per request: registration now completes directly without an OTP step.
// Keeping a minimal placeholder to avoid accidental import/build errors in other files.
import React from 'react';

const RemovedOtpVerify = () => null;

export default RemovedOtpVerify;